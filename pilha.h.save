#include <stdio.h>
#include <stdlib.h>

typedef struct _pilha {
    int* data;
    int top;
    int tam;
} Pilha;

void empurra(Pilha *ph, int valor);
int popa(Pilha *ph);
int olha(Pilha *ph);
int vazia(Pilha *ph);
Pilha* init(int tam);

